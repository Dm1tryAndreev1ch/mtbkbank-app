# Прямые запросы к API из браузера (обход Vite proxy) — логин всегда получает JSON с Express.
# Порт должен совпадать с backend (см. backend/.env PORT или 3000 по умолчанию).
VITE_API_ORIGIN=http://127.0.0.1:3000
